/**
 * Generated by orval. Do not edit manually.
 * e621 API
 * e621ng 2da4b89799b7799a4920adfe14112939976b4fcd
 */

export interface UpdateStaffUserBodyUser {
  /** A new username; a value differing from the current one files an administrative name change request */
  name?: string;
  /** A new email address. Only accepted from BD staff */
  email?: string;
  /** The user's "About" profile section */
  profile_about?: string;
  /** The user's art information profile section */
  profile_artinfo?: string;
  /** The base upload limit for the user */
  base_upload_limit?: number;
  /** Whether the user's activity is hidden from other users */
  enable_privacy_mode?: boolean;
  /** The title shown beside the user's name */
  custom_title?: string;
  /** The user's upload karma; the difference from the current value is applied as a staff override */
  upload_karma?: number;
  /** Whether the account is email-verified. Only honoured for BD staff */
  verified?: boolean;
  /** The user's new access level (0 Anonymous, 10 Blocked, 20 Member, 30 Privileged, 40 Former Staff, 50 Staff, 60 Janitor, 70 Moderator, 80 Admin) */
  level?: number;
  /** Whether the user may approve posts. Only applied when `level` is also sent */
  can_approve_posts?: boolean;
  /** Whether the user bypasses the upload queue. Only applied when `level` is also sent */
  can_upload_free?: boolean;
  /** Whether the user holds the tag warden flag. Only applied when `level` is also sent */
  tag_warden?: boolean;
  /** Whether the user is barred from flagging posts. Only applied when `level` is also sent */
  no_flagging?: boolean;
  /** Whether the user may use the replacements beta. Only applied when `level` is also sent */
  replacements_beta?: boolean;
  /** Whether the user has a raised favorite limit. Only applied when `level` is also sent */
  raised_favorite_limit?: boolean;
}
