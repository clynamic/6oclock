/**
 * Generated by orval. Do not edit manually.
 * e621 API
 * e621ng 2da4b89799b7799a4920adfe14112939976b4fcd
 */
import type { PostRating } from './postRating';

export interface UpdatePostBodyPost {
  /** The complete new tag string. Ignored when `tag_string_diff` is present. */
  tag_string?: string;
  /** The tag string the edit was based on, used for conflict detection */
  old_tag_string?: string;
  /** Tags to add and remove, with removals prefixed by `-` */
  tag_string_diff?: string;
  /** Sources to add and remove, with removals prefixed by `-` */
  source_diff?: string;
  /** The ID of the parent post */
  parent_id?: number;
  /** The parent ID the edit was based on, used for conflict detection */
  old_parent_id?: number;
  /** The complete new source list, newline separated. Ignored when `source_diff` is present. */
  source?: string;
  /** The source list the edit was based on, used for conflict detection */
  old_source?: string;
  /** The post description */
  description?: string;
  /** The description the edit was based on, used for conflict detection */
  old_description?: string;
  rating?: PostRating;
  old_rating?: PostRating;
  /** The reason recorded for this edit */
  edit_reason?: string;
  /** Whether the rating is locked. Must be Privileged+ to send. */
  is_rating_locked?: boolean;
  /** Whether notes are locked. Must be Staff+ to send. */
  is_note_locked?: boolean;
  /** The post's background colour. Must be Staff+ to send. */
  bg_color?: string;
  /** Whether commenting is locked. Must be Moderator+ to send. */
  is_comment_locked?: boolean;
  /** Whether the post status is locked. Must be Admin+ to send. */
  is_status_locked?: boolean;
  /** Whether comments are hidden. Must be Admin+ to send. */
  is_comment_disabled?: boolean;
  /** Tags that cannot be removed. Must be Admin+ to send. */
  locked_tags?: string;
  /** Whether the post is hidden from anonymous visitors. Must be Admin+ to send. */
  hide_from_anonymous?: boolean;
  /** Whether the post is hidden from search engines. Must be Admin+ to send. */
  hide_from_search_engines?: boolean;
  /** Whether the post's favorites list is hidden. Must be Admin+ to send. */
  hide_favorites_list?: boolean;
}
