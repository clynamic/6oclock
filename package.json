{
  "name": "6oclock",
  "private": true,
  "description": "e6 staff dashboard",
  "license": "MIT",
  "author": "binaryfloof",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d --wait && concurrently -k -n api,worker,web -c blue,green,magenta \"npm run api\" \"npm run worker\" \"npm run web\"",
    "api": "npm run dev -w @6oclock/api",
    "web": "npm run dev -w @6oclock/web",
    "build:api": "npm run build -w @6oclock/api",
    "build:web": "npm run build -w @6oclock/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "worker": "npm run dev:worker -w @6oclock/api"
  },
  "devDependencies": {
    "commander": "~14.0.1",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=11.10.0"
  },
  "overrides": {
    "typescript": "^6.0.3"
  }
}
